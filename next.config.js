/** @type {import('next').NextConfig} */
const nextConfig = {
  /* 
    Next.js 16 with Turbopack may handle linting/types differently.
    If you need to ignore these during build, consider using the CLI flags 
    or the modern configuration keys if they have changed.
  */
}

module.exports = nextConfig
